server.port=8080
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/transaction_core}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.jpa.show-sql=true

